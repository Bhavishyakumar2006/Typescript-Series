// Inferences -: there TS understands what should type of value assign in variable.

let name = "bhavishya"

let marks = Math.random() > 0 ? 5 : '10'

// Annotations -: there TS have to tell or annotate the type of variable.

let chai:string = 'Masala chai'
chai = "Ginger chai"

// Some Annotations example -: number, string, boolean